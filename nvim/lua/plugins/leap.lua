-- Currently not in use. Need to confiugure properly

return {
    "ggandor/leap.nvim",
    dependencies = { "tpope/vim-repeat" },
      keys = {
        --{ "<leader>f", "<cmd>Telescope help_tags<cr>", desc = "Leap" },
    }
}

